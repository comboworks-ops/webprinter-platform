# Agent Product Handoff Template

Use these files when another agent scrapes a supplier product for later WebPrinter import.

Required:

- `product.json`
- `prices.csv`
- `sources.json`

Optional:

- `description.md`

When the package is ready, provide it back in this chat and ask for validation/import.

Primary spec:

- `docs/AGENT_PRODUCT_HANDOFF_SPEC.md`

Related import foundation:

- `docs/PRODUCT_BLUEPRINT_SPEC.md`
